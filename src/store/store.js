import { configureStore } from '@reduxjs/toolkit';
// import projectReducer from './slices/projectSlice.js';

export const store = configureStore({
  reducer: {
    // articles: projectReducer,
    // add reducers here ...
  },
});
